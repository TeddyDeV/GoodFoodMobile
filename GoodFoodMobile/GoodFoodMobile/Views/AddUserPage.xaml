<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GoodFoodMobile.ViewModels"
             x:Class="GoodFoodMobile.Views.AddUserPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Retour" Command="{Binding LoginPageCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Label Text="Création de votre compte" FontSize="Large" />
                 <Label/>
                 <Entry Placeholder="Nom"></Entry>
                 <Entry Placeholder="Prénom"></Entry>
                 <Entry Placeholder="Email"></Entry>
                 <Entry Placeholder="Mot de passe"></Entry>
                 <Entry Placeholder="Rue"></Entry>
                 <Entry Placeholder="Code postal"></Entry>
                 <Entry Placeholder="Ville"></Entry>
                 <Entry Placeholder="Numéro de téléphone"></Entry>
                 <Label></Label>
                 <Button Text="Valider" BackgroundColor="Green" ></Button>
                 <!--Command="{Binding SubmitUserCommand}"-->           
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>